import { DataTypes } from 'sequelize';
import { Column, HasMany, Table, Model } from 'sequelize-typescript';

@Table({
  timestamps: true,
  tableName: '{{lowerCase name}}',
})
class {{pascalCase name}}DTO extends Model<{{pascalCase name}}, Omit<{{pascalCase name}}, "id">> {
  @Column({
    type: DataTypes.STRING,
    allowNull: false,
  })
  name!: string;
}

export default {{pascalCase name}}DTO;